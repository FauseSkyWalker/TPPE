{
  "name": "agencia-viagens",
  "version": "1.0.0",
  "scripts": {
    "lint": "flake8 app/ tests/",
    "format": "black app/ tests/",
    "format:check": "black --check app/ tests/",
    "test": "pytest tests/ -v --cov=app"
  }
}
